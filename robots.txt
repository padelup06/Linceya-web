User-agent: *
Allow: /

# Pages transactionnelles : on les laisse crawler pour que Google voie le
# <meta name="robots" content="noindex"> dans leur <head>. Si on les bloque
# ici, Google ne peut pas lire le noindex et flag "Bloquée par robots.txt"
# dans Search Console.

# Endpoints API (Cloudflare Workers) : pas indexables, retournent du JSON
# ou des erreurs en GET. Pas de page HTML à crawler donc on bloque ici.
Disallow: /api/

Sitemap: https://linceya.com/sitemap.xml
